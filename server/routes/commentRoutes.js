import {Router} from "express";import Comment from "../models/Comment.js";import Task from "../models/Task.js";import Project from "../models/Project.js";import {auth} from "../middleware/auth.js";const router=Router();router.use(auth);async function access(task,uid){const p=await Project.findById(task.project);return p&&(p.owner.equals(uid)||p.members.some(m=>m.equals(uid)))?p:null}router.get("/task/:taskId",async(req,res)=>{const task=await Task.findById(req.params.taskId);if(!task||!(await access(task,req.user._id)))return res.status(403).json({message:"Access denied"});res.json({comments:await Comment.find({task:task._id}).populate("user","name email").sort("createdAt")})});router.post("/",async(req,res)=>{const task=await Task.findById(req.body.task);const p=task&&await access(task,req.user._id);if(!task||!p)return res.status(403).json({message:"Access denied"});const c=await Comment.create({task:task._id,user:req.user._id,content:req.body.content});const populated=await c.populate("user","name email");req.app.get("io").to(`project:${p._id}`).emit("commentAdded",populated);res.status(201).json({comment:populated})});export default router;